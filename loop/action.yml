name: "LoopPilot Action"
description: "AI review-fix loop for GitHub pull requests"

# Composite action wiring the three-step Workflow B repair pipeline:
#   1. pre-fix         — label gate, state guards, debounce, judge, prompt build
#   2. claude-code-action — repo-level repair executor (anthropics/claude-code-action@v1)
#   3. post-fix        — scope check, CHECK_COMMAND, commit/push, state update,
#                        re-request Codex review
#
# pre-fix decides whether the iteration runs via `should_run`. When false the
# claude-code-action and post-fix steps are skipped — pre-fix has already
# finalized state (done / stopped) for this trigger.

inputs:
  github-token:
    description: "GitHub token with contents:write, pull-requests:write, issues:write"
    required: true
  codex-review-request-token:
    description: "Optional token from a Codex-connected user for posting @codex review"
    required: false
    default: ""
  looppilot-push-token:
    description: "Optional token used only for repair commit pushes; use a machine-user PAT or GitHub App token when required checks must run on repair commits"
    required: false
    default: ""
  anthropic-api-key:
    description: "Anthropic API key for claude-code-action. Set this OR claude-code-oauth-token (exactly one). See docs/operations/security.md (認証)."
    required: false
    default: ""
  claude-code-oauth-token:
    description: "Claude Code subscription OAuth token (Pro / Max, generated via `claude setup-token`). Set this OR anthropic-api-key (exactly one). See docs/operations/security.md (認証)."
    required: false
    default: ""
  pr-number:
    description: "Pull request number"
    required: true
  pr-head-ref:
    description: "PR head branch ref name"
    required: true
  pr-title:
    description: "PR title"
    required: false
    default: ""
  trigger-comment-id:
    description: "ID of the Codex summary comment or PR review that triggered this run"
    required: false
    default: "0"
  trigger-comment-body:
    description: "Body of the Codex summary comment or PR review that triggered this run"
    required: false
    default: ""
  trigger-user-login:
    description: "GitHub login of the user who posted the trigger comment or review"
    required: false
    default: ""
  trigger-event-name:
    description: "GitHub event name that fired the trigger (`issue_comment` or `pull_request_review`). Used by pre-fix to disambiguate the `lastProcessedReviewId` namespace (TY-301 #2). Empty default keeps the pre-TY-301 id-only dedup behaviour."
    required: false
    default: ""
  max-review-iterations:
    description: "Maximum number of review-fix iterations"
    required: false
    default: "20"
  debounce-seconds:
    description: "Seconds to wait after Codex summary comment before collecting inline comments"
    required: false
    default: "90"
  check-command:
    description: "Shell command to run after applying fixes (test/lint/typecheck)"
    required: false
    default: "npm run check"
  build-command:
    description: "Optional shell command run by post-fix after CHECK_COMMAND succeeds, before staging (TY-281). Use this to regenerate build artifacts that are committed to the repo (e.g. `dist/`) so the auto-fix commit cannot drift out of sync with `src/`. Empty default skips the step; chain multiple steps with `&&` or wrap in an npm script."
    required: false
    default: ""
  codex-bot-login:
    description: "GitHub login of the Codex bot"
    required: false
    default: "chatgpt-codex-connector[bot]"
  codex-ack-timeout-seconds:
    description: "TY-334: per-attempt seconds post-fix polls for a Codex ACK (👀 reaction or new Codex activity) after posting @codex review. 0 disables polling. Max 120 so timeout × (max-reposts + 1) stays under the job timeout."
    required: false
    default: "90"
  codex-ack-poll-interval-seconds:
    description: "TY-334: seconds between ACK polls. Default 15."
    required: false
    default: "15"
  codex-ack-max-reposts:
    description: "TY-334: hard cap on @codex review reposts when no ACK arrives. After this many, post-fix demotes to stopped/codex_request_failed. Default 2, max 3."
    required: false
    default: "2"
  auto-retry-escalate:
    description: "ES-496: when 'true', a base-tier max_turns_exceeded stop re-requests @codex review and retries once at the escalated tier without a manual /restart-review (one-shot). Default 'false'."
    required: false
    default: "false"
  stabilize-interval-seconds:
    description: "Polling interval (seconds) for comment count stabilization safeguard"
    required: false
    default: "10"
  stabilize-count:
    description: "Number of consecutive stable polls before considering comments settled"
    required: false
    default: "3"
  looppilot-label:
    description: "Label name required to run loop-pilot loop. Empty falls back to 'loop-pilot'. Note: the label must exist on the repository and be applied to the PR (TY-293 #2) — otherwise the workflow `if:` evaluates to false and no run is generated."
    required: false
    default: ""
  looppilot-full-auto:
    description: "Set to 'true' to disable the label gate and run LoopPilot on every non-fork ready PR."
    required: false
    default: "false"
  looppilot-restart-roles:
    description: "Comma-separated roles allowed to run /restart-review."
    required: false
    default: "author,write,maintain,admin"
  claude-code-model-base:
    description: "Base tier model used when no escalation signal fires. See docs/operations/security.md (model selection). Set this equal to claude-code-model-escalated to operate without tiering."
    required: false
    default: "claude-sonnet-4-6"
  claude-code-model-escalated:
    description: "Escalated tier model used when a P0 finding is present, the previous iteration's CHECK_COMMAND failed, or the same findings hash repeats at the base tier."
    required: false
    default: "claude-opus-4-6"
  claude-code-max-turns:
    description: "Value forwarded to claude-code-action's --max-turns argument."
    required: false
    default: "40"
  github-repository:
    description: "Repository in owner/name format (auto-detected in GitHub Actions)"
    required: false
    default: ""
  auto-merge-on-clean:
    description: "Set to 'true' to auto-merge (squash) the PR when the loop terminates with done/no_findings. Default 'false' preserves manual merge. Auto-merge waits for every other workflow run on HEAD to settle, refuses to merge if any concluded as failure/cancelled/timed_out/action_required/startup_failure/stale, and bails if HEAD changes during the wait (TY-277). Calls `gh pr merge --auto --squash` which requires Repository Settings → General → 'Allow auto-merge' to be enabled — without it the merge call fails and mergeIfChecksPass silent-skips with a warning (TY-288)."
    required: false
    default: "false"
  auto-merge-poll-seconds:
    description: "Poll interval (seconds) for the CI wait before auto-merge (TY-277). Default 15. Lower bound 1."
    required: false
    default: "15"
  auto-merge-timeout-minutes:
    description: "Hard timeout (minutes) on the CI wait before auto-merge skips with a warning (TY-277). Default 10. Lower bound 1."
    required: false
    default: "10"
  severity-threshold:
    description: "Severity threshold for the auto-fix pipeline. One of P0|P1|P2|P3. Default 'P3' includes every severity (P0/P1/P2/P3); 'P2' preserves the historical scope (P0/P1/P2 only, skip P3); 'P1'/'P0' narrow further. Invalid values fall back to P3 with a warning (TY-256). NOTE (TY-268 #23): the parser fallback only catches P0 / P1 keywords without an explicit badge — P2/P3 findings without a badge are counted as `unparseable` and skipped, which matters at the default P3 threshold."
    required: false
    default: "P3"
  looppilot-block-paths:
    description: "`.gitignore`-style block-path spec (TY-271). Comma-separated entries: `secrets/` (dir prefix), `Justfile` (exact file), `!Makefile` (un-block a default). `!.github/...` is ignored — `.github/` is locked. Empty default keeps the built-in `DEFAULT_BLOCK_PATTERNS`. See docs/operations/scope-policy.md."
    required: false
    default: ""
  scope-max-files:
    description: "Max files allowed in a single auto-fix diff. Empty / 0 keeps the default of 20. Leave empty so `LOOPPILOT_SCOPE_MAX_FILES` Repository variable can override; `core.getInput` treats `\"0\"` as non-empty and would shadow the env-var fallback."
    required: false
    default: ""
  scope-max-lines:
    description: "Max total added+deleted lines allowed in a single auto-fix diff. Empty / 0 keeps the default of 1000. Leave empty so `LOOPPILOT_SCOPE_MAX_LINES` Repository variable can override."
    required: false
    default: ""
  looppilot-state-comment-authors:
    description: "Comma-separated GitHub logins trusted to write the hidden state comment. Defaults to 'github-actions[bot]'. Set to your GitHub App slug or machine-user login when the workflow runs with a non-default token."
    required: false
    default: ""
  show-full-output:
    description: "Forward to claude-code-action's show_full_output. When 'true', the full Claude Code session log is printed to the Actions run log. Defaults to false; set to 'true' when debugging (ES-429)."
    required: false
    default: "false"

runs:
  using: "composite"
  steps:
    - name: Pre-fix (Phase 0–3 prep)
      id: pre
      # External-consumption fix: in a composite action a `./` local ref resolves
      # against the CONSUMER's GITHUB_WORKSPACE, not this action's own checkout, so
      # `./loop/pre-fix` breaks for adopters using `edereship/loop-pilot/loop@v1`
      # ("Can't find action.yml under <consumer>/loop/pre-fix"). Pin to the
      # published path@ref so the sub-action always loads from THIS repo. Keep the
      # tag in sync with releases (currently v1). Do NOT revert to `./loop/pre-fix`.
      uses: edereship/loop-pilot/loop/pre-fix@v1
      with:
        github-token: ${{ inputs.github-token }}
        codex-review-request-token: ${{ inputs.codex-review-request-token }}
        anthropic-api-key: ${{ inputs.anthropic-api-key }}
        claude-code-oauth-token: ${{ inputs.claude-code-oauth-token }}
        pr-number: ${{ inputs.pr-number }}
        pr-head-ref: ${{ inputs.pr-head-ref }}
        pr-title: ${{ inputs.pr-title }}
        trigger-comment-id: ${{ inputs.trigger-comment-id }}
        trigger-comment-body: ${{ inputs.trigger-comment-body }}
        trigger-user-login: ${{ inputs.trigger-user-login }}
        trigger-event-name: ${{ inputs.trigger-event-name }}
        max-review-iterations: ${{ inputs.max-review-iterations }}
        debounce-seconds: ${{ inputs.debounce-seconds }}
        check-command: ${{ inputs.check-command }}
        build-command: ${{ inputs.build-command }}
        codex-bot-login: ${{ inputs.codex-bot-login }}
        # TY-334: forward ACK-poll tuning so the /restart-review re-post is
        # monitored for a Codex acknowledgement (pre-fix handles restart).
        codex-ack-timeout-seconds: ${{ inputs.codex-ack-timeout-seconds }}
        codex-ack-poll-interval-seconds: ${{ inputs.codex-ack-poll-interval-seconds }}
        codex-ack-max-reposts: ${{ inputs.codex-ack-max-reposts }}
        stabilize-interval-seconds: ${{ inputs.stabilize-interval-seconds }}
        stabilize-count: ${{ inputs.stabilize-count }}
        looppilot-label: ${{ inputs.looppilot-label }}
        looppilot-full-auto: ${{ inputs.looppilot-full-auto }}
        looppilot-restart-roles: ${{ inputs.looppilot-restart-roles }}
        github-repository: ${{ inputs.github-repository }}
        claude-code-model-base: ${{ inputs.claude-code-model-base }}
        claude-code-model-escalated: ${{ inputs.claude-code-model-escalated }}
        auto-merge-on-clean: ${{ inputs.auto-merge-on-clean }}
        auto-merge-poll-seconds: ${{ inputs.auto-merge-poll-seconds }}
        auto-merge-timeout-minutes: ${{ inputs.auto-merge-timeout-minutes }}
        severity-threshold: ${{ inputs.severity-threshold }}
        looppilot-state-comment-authors: ${{ inputs.looppilot-state-comment-authors }}
        # TY-350: pre-fix builds the repair prompt's `## Scope Policy` section
        # from these (main-pre-fix.ts `buildScopePolicy`), so Claude sees the
        # operator's effective block-list / size budget up front and avoids
        # producing diffs that post-fix would only revert. The post-fix step
        # below receives the same three inputs for the authoritative
        # enforcement check; forwarding them to BOTH keeps prompt and
        # enforcement in sync (TY-350).
        looppilot-block-paths: ${{ inputs.looppilot-block-paths }}
        scope-max-files: ${{ inputs.scope-max-files }}
        scope-max-lines: ${{ inputs.scope-max-lines }}

    # ES-427: export credentials to GITHUB_ENV only when non-empty.
    #
    # claude-code-action@v1 unconditionally sets
    #   ANTHROPIC_API_KEY: ${{ inputs.anthropic_api_key || env.ANTHROPIC_API_KEY }}
    # in its step env block. When both the input and the env var are "",
    # the expression evaluates to "" and ANTHROPIC_API_KEY="" is exported
    # to the CLI process. The Claude CLI treats an empty API key as
    # "credential present" — it wins the precedence slot over
    # CLAUDE_CODE_OAUTH_TOKEN and authenticates with an empty key, which
    # fails. OAuth-only deployments are then 100% broken.
    #
    # Workaround: remove the with: inputs (so inputs.anthropic_api_key
    # defaults to "" in the upstream action) and write ONLY non-empty
    # credentials to GITHUB_ENV. For API-key users env.ANTHROPIC_API_KEY
    # is now the real key and the upstream || picks it up correctly. For
    # OAuth users env.ANTHROPIC_API_KEY stays unset; the upstream env
    # block still evaluates to "", but LoopPilot no longer actively
    # contributes to the problem — the full fix requires an upstream
    # change to skip setting ANTHROPIC_API_KEY when both sources are empty.
    - name: Export Claude credentials to GITHUB_ENV (ES-427)
      if: steps.pre.outputs.should_run == 'true'
      shell: bash
      run: |
        if [ -n "$LP_ANTHROPIC_API_KEY" ]; then
          {
            echo "ANTHROPIC_API_KEY<<LP_CREDENTIAL_DELIM"
            echo "$LP_ANTHROPIC_API_KEY"
            echo "LP_CREDENTIAL_DELIM"
          } >> "$GITHUB_ENV"
        fi
        if [ -n "$LP_CLAUDE_CODE_OAUTH_TOKEN" ]; then
          {
            echo "CLAUDE_CODE_OAUTH_TOKEN<<LP_CREDENTIAL_DELIM"
            echo "$LP_CLAUDE_CODE_OAUTH_TOKEN"
            echo "LP_CREDENTIAL_DELIM"
          } >> "$GITHUB_ENV"
        fi
      env:
        LP_ANTHROPIC_API_KEY: ${{ inputs.anthropic-api-key }}
        LP_CLAUDE_CODE_OAUTH_TOKEN: ${{ inputs.claude-code-oauth-token }}

    - name: Claude Code Action (repo-level repair)
      id: claude
      if: steps.pre.outputs.should_run == 'true'
      uses: anthropics/claude-code-action@v1
      with:
        # ES-427: credentials are exported to GITHUB_ENV by the preceding
        # step — only when non-empty — so claude-code-action picks them up
        # via its env.ANTHROPIC_API_KEY / env.CLAUDE_CODE_OAUTH_TOKEN
        # fallback. Do NOT re-add anthropic_api_key or claude_code_oauth_token
        # here; passing "" re-introduces the empty-key precedence bug.
        github_token: ${{ inputs.github-token }}
        prompt: ${{ steps.pre.outputs.prompt }}
        # Tools / model / turn budget are governed by docs/operations/security.md
        # ("Claude Code Action 実行制御"). Bash is allow-listed to the exact
        # commands required for verification — git commit / git push are
        # intentionally excluded so post-fix retains exclusive commit authority.
        #
        # The Bash allowlist is composed dynamically by pre-fix
        # (`allowed_bash_tools`): a baseline (npm setup commands + git
        # read-only) plus the CHECK_COMMAND when it passes the safety
        # whitelist. This lets downstream repositories use non-npm
        # CHECK_COMMAND (pnpm / yarn / pytest / make / …) without
        # exhausting --max-turns on a permission-denied verification step
        # (TY-238). Pre-fix rejects unsafe commands and falls back to the
        # baseline; see src/check-command-allowlist.ts for the rules.
        #
        # `--disallowedTools` explicitly blocks the MCP server tools that
        # could otherwise commit / push / delete files directly and bypass
        # the post-fix scope check + CHECK_COMMAND gate. The corresponding
        # MCP servers are already gated off by claude-code-action's defaults
        # (github_file_ops requires `use_commit_signing: true`; the full
        # github MCP server requires `mcp__github__*` entries in allowedTools),
        # but listing them here keeps the intent durable against future
        # default changes upstream.
        #
        # The `github_comment` / `github_inline_comment` base tools are blocked
        # too. claude-code-action posts / updates comments as the same
        # `github-actions[bot]` identity that the hidden-state trust filter
        # (TY-272 #A, `state-manager.ts` buildTrustedAuthorJqFilter) treats as
        # the single authoritative writer. An IPI-influenced or compromised
        # agent could therefore update its tracking comment to begin with the
        # `looppilot-state` visible header + marker and forge the hidden
        # state JSON; `readState` takes the newest matching trusted comment, so
        # the forgery would win and corrupt loop accounting (iteration count /
        # loop detection → unbounded re-review cost). The loop posts every
        # status / `@codex review` comment itself from pre-/post-fix, so the
        # agent never needs comment tools — blocking them closes the forgery
        # vector at the source without affecting the repair.
        claude_args: >-
          --model ${{ steps.pre.outputs.model }}
          --max-turns ${{ inputs.claude-code-max-turns }}
          --allowedTools "Read,Glob,Grep,Edit,Write,TodoWrite,${{ steps.pre.outputs.allowed_bash_tools }}"
          --disallowedTools "WebFetch,WebSearch,Task,NotebookEdit,mcp__github_file_ops__commit_files,mcp__github_file_ops__delete_files,mcp__github__create_or_update_file,mcp__github__push_files,mcp__github__delete_file,mcp__github__create_branch,mcp__github__create_pull_request,mcp__github__update_pull_request,mcp__github__merge_pull_request,mcp__github__fork_repository,mcp__github_comment__update_claude_comment,mcp__github_inline_comment__create_inline_comment"
        # The Codex bot is the canonical Workflow B trigger
        # (pull_request_review.submitted from chatgpt-codex-connector[bot]),
        # so claude-code-action's bot-actor guard must allow it explicitly.
        # `*` is intentionally avoided; only the configured Codex bot login
        # is allowed through.
        show_full_output: ${{ inputs.show-full-output }}
        allowed_bots: ${{ inputs.codex-bot-login }}

    # ES-427 (P2): clear credentials from GITHUB_ENV immediately after
    # claude-code-action — before post-fix runs. $GITHUB_ENV is job-scoped,
    # so without this step the secret leaks to post-fix child processes
    # (git commit/push, CHECK_COMMAND, BUILD_COMMAND) and their environment
    # (e.g. Git hooks on self-hosted runners). Placed here rather than after
    # post-fix so the credential window is as narrow as possible. The
    # always() guard ensures cleanup runs even if claude-code-action fails,
    # since post-fix also runs on failure.
    - name: Clear Claude credentials from GITHUB_ENV (ES-427)
      if: always() && steps.pre.outputs.should_run == 'true'
      shell: bash
      run: |
        echo "ANTHROPIC_API_KEY=" >> "$GITHUB_ENV"
        echo "CLAUDE_CODE_OAUTH_TOKEN=" >> "$GITHUB_ENV"

    - name: Post-fix (scope check + CHECK_COMMAND + commit + state)
      if: always() && steps.pre.outputs.should_run == 'true'
      # See the pre-fix step above: pinned path@ref so adopters can consume this
      # composite action (a `./` local ref would resolve to the consumer repo).
      uses: edereship/loop-pilot/loop/post-fix@v1
      with:
        github-token: ${{ inputs.github-token }}
        codex-review-request-token: ${{ inputs.codex-review-request-token }}
        looppilot-push-token: ${{ inputs.looppilot-push-token }}
        pr-number: ${{ inputs.pr-number }}
        pr-head-ref: ${{ steps.pre.outputs.pr_head_ref }}
        pr-title: ${{ inputs.pr-title }}
        comment-id: ${{ steps.pre.outputs.comment_id }}
        iteration: ${{ steps.pre.outputs.iteration }}
        # TY-337: forward the iteration cap so post-fix's status-comment
        # **Iterations** header shows the operator-configured cap instead of
        # the default 20 (workflow → composite is already wired at
        # loop.yml `max-review-iterations`).
        max-review-iterations: ${{ inputs.max-review-iterations }}
        trigger-comment-id: ${{ steps.pre.outputs.trigger_comment_id }}
        check-command: ${{ steps.pre.outputs.check_command }}
        build-command: ${{ inputs.build-command }}
        action-outcome: ${{ steps.claude.outcome }}
        action-execution-file: ${{ steps.claude.outputs.execution_file }}
        github-repository: ${{ inputs.github-repository }}
        looppilot-block-paths: ${{ inputs.looppilot-block-paths }}
        scope-max-files: ${{ inputs.scope-max-files }}
        scope-max-lines: ${{ inputs.scope-max-lines }}
        looppilot-state-comment-authors: ${{ inputs.looppilot-state-comment-authors }}
        # TY-334: forward Codex bot login + ACK-poll tuning so post-fix can
        # recover a silently-dropped @codex review request after a repair.
        codex-bot-login: ${{ inputs.codex-bot-login }}
        codex-ack-timeout-seconds: ${{ inputs.codex-ack-timeout-seconds }}
        codex-ack-poll-interval-seconds: ${{ inputs.codex-ack-poll-interval-seconds }}
        codex-ack-max-reposts: ${{ inputs.codex-ack-max-reposts }}
        # ES-496: opt-in escalated-tier auto-retry on a base-tier max_turns stop.
        auto-retry-escalate: ${{ inputs.auto-retry-escalate }}
